# Google AutoComplete Python Project

A Python program that loads data from an archive and simulates the autocomplete functionality
of Google search.

Start the program by running the "user_interface.py" python module.